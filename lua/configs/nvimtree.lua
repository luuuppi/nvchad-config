return {
  filters = {
    dotfiles = false,
    git_ignored = false,
  },
  view = {
    number = true,
    relativenumber = true,
  },
  diagnostics = {
    enable = true,
    show_on_dirs = true,
  },
}
